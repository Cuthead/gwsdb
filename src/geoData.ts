// The canonical data table lives in public/static/geoData.js -- it's served
// directly to the browser (see home.js) as well as imported here for the
// server-side (Pages Functions) code. Edit the .js file, not this one.
export * from "../public/static/geoData.js";
